import { memo } from "react";
// import { Link } from "react-router-dom";

const InfoPage = () => {
    return (
        <div className="w-full mx-auto mt-4 mb-4">InfoPage</div>
    );
};

export default memo(InfoPage);